{
  "name": "@aims/validation",
  "version": "0.0.1",
  "private": true,
  "description": "Shared Zod schemas — the single source of truth for contract validation across web, api, and worker.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"(tests added as schemas accrue)\" && exit 0",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
