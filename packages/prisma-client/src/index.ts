/**
 * @aims/prisma-client
 *
 * Tenant-isolated Prisma client per ADR-0002 (two-layer tenant isolation).
 *
 * SLICE A TASK: This is a placeholder. Real implementation arrives in:
 *   - Task 1.3: Prisma schema + migrations (database/schema.prisma → prisma/schema.prisma here)
 *   - Task 1.4: Prisma Client Extension that injects tenantId on every query
 *   - Task 1.5: RLS policies applied via migration
 *
 * See VERTICAL-SLICE-PLAN.md §4 Week 1 for sequencing.
 */

export const PLACEHOLDER = true as const;
