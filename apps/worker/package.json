{
  "name": "@aims/worker",
  "version": "0.0.1",
  "private": true,
  "description": "NestJS worker tier (ADR-0003): SQS consumers, PDF rendering, audit-log chain append, scheduled jobs.",
  "type": "module",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"(tests added in Task 4.4+ — outbox → SQS → worker integration)\" && exit 0",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@aims/encryption": "workspace:*",
    "@aims/prisma-client": "workspace:*",
    "@aims/validation": "workspace:*",
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/client-sqs": "^3.658.0",
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@nestjs/platform-express": "^10.4.4",
    "@nestjs/schedule": "^4.1.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "tslib": "^2.7.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.5",
    "@nestjs/schematics": "^10.2.0",
    "@types/node": "^22.7.0"
  }
}
