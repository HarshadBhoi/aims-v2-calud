{
  "name": "@aims/api",
  "version": "0.0.1",
  "private": true,
  "description": "Fastify + tRPC hot-path API (ADR-0003). NestJS lives in apps/worker, NOT here.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo \"(tests added per task — first in Task 2.4 when tRPC middleware lands)\" && exit 0",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@aims/encryption": "workspace:*",
    "@aims/prisma-client": "workspace:*",
    "@aims/validation": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1",
    "@fastify/sensible": "^6.0.1",
    "fastify": "^5.0.0",
    "pino": "^9.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0"
  }
}
