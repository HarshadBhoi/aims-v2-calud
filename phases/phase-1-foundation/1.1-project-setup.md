# 1.1 — Project Setup & Monorepo

> **⚠ Reconciliation 2026-04-28**: PARTIALLY SUPERSEDED. Task list still 80% useful for monorepo bootstrap, but says "all NestJS"; [ADR-0003](../../references/adr/0003-nestjs-scoped-to-workers.md) split the runtime — Fastify on the request path, NestJS workers only. Slice A's monorepo is shipped at [`apps/{api,web,worker}`](../../apps/) + [`packages/{prisma-client,encryption,validation}`](../../packages/). See [VERTICAL-SLICE-PLAN.md §5](../../VERTICAL-SLICE-PLAN.md).

**Parent**: [Phase 1 — Foundation](OVERVIEW.md)
**Estimated effort**: 2-3 days

---

## Objective

Initialize the Turborepo monorepo with Next.js frontend, NestJS backend, shared packages, and development tooling.

---

## Tasks

### 1.1.1 — Initialize Monorepo
- [ ] Create new repository `aims-v2`
- [ ] Initialize pnpm workspace (`pnpm-workspace.yaml`)
- [ ] Configure Turborepo (`turbo.json`) with build/dev/lint/test pipelines
- [ ] Set up root `package.json` with workspace scripts
- [ ] Configure root `tsconfig.json` with path aliases

### 1.1.2 — Frontend App (Next.js 15)
- [ ] Create `apps/web` with `create-next-app` (App Router, TypeScript, Tailwind v4)
- [ ] Configure `next.config.ts` (transpile workspace packages, environment variables)
- [ ] Set up folder structure:
  ```
  apps/web/
  ├── app/                    # App Router pages
  │   ├── (auth)/             # Auth pages (login, register, forgot-password)
  │   ├── (dashboard)/        # Authenticated pages
  │   │   ├── layout.tsx      # Shell with sidebar + topbar
  │   │   ├── page.tsx        # Dashboard home
  │   │   ├── engagements/
  │   │   ├── findings/
  │   │   ├── reports/
  │   │   └── admin/
  │   ├── layout.tsx          # Root layout (providers, fonts)
  │   └── globals.css         # Tailwind base
  ├── components/             # App-specific components
  ├── hooks/                  # Custom hooks
  ├── lib/                    # Utilities, API client
  └── providers/              # Context providers
  ```
- [ ] Install and configure Shadcn/ui (`npx shadcn@latest init`)
- [ ] Set up environment variables (`.env.local`, `.env.example`)

### 1.1.3 — Backend App (NestJS)
- [ ] Create `apps/api` with NestJS CLI (TypeScript strict)
- [ ] Configure module structure:
  ```
  apps/api/
  ├── src/
  │   ├── modules/
  │   │   ├── auth/           # Authentication module
  │   │   ├── users/          # User management
  │   │   ├── tenants/        # Multi-tenancy
  │   │   ├── engagements/    # Audit engagements
  │   │   ├── findings/       # Findings & recommendations
  │   │   ├── workpapers/     # Document management
  │   │   ├── workflows/      # Approval engine
  │   │   ├── standards/      # Standards abstraction
  │   │   └── reports/        # Report generation
  │   ├── common/
  │   │   ├── guards/         # Auth, RBAC, tenant guards
  │   │   ├── interceptors/   # Logging, audit trail
  │   │   ├── filters/        # Exception handling
  │   │   ├── decorators/     # Custom decorators
  │   │   └── middleware/     # Tenant context, request logging
  │   ├── config/             # App configuration
  │   ├── prisma/             # Prisma service
  │   └── main.ts             # Bootstrap
  ├── test/                   # E2E tests
  └── nest-cli.json
  ```
- [ ] Set up tRPC integration with NestJS (`@trpc/server`, `@trpc/client`)
- [ ] Configure CORS, helmet, rate limiting, compression
- [ ] Set up environment variables

### 1.1.4 — Shared Packages
- [ ] Create `packages/shared-types` — TypeScript interfaces shared between frontend and backend
- [ ] Create `packages/validation` — Zod schemas for forms and API validation
- [ ] Create `packages/utils` — Common utilities (date, format, permission helpers)
- [ ] Create `packages/standard-packs` — Audit standard definitions (start with structure, populate later)
- [ ] Create `packages/pdf-engine` — PDF generation service (port pdfmake logic from v1)

### 1.1.5 — Development Tooling
- [ ] ESLint config (shared, with strict TypeScript rules)
- [ ] Prettier config (shared)
- [ ] Husky + lint-staged (pre-commit hooks)
- [ ] Commitlint (conventional commits)
- [ ] Vitest config (shared, workspace-aware)
- [ ] `.editorconfig`
- [ ] VS Code workspace settings + recommended extensions

### 1.1.6 — Docker Setup
- [ ] `Dockerfile.web` (multi-stage build for Next.js)
- [ ] `Dockerfile.api` (multi-stage build for NestJS)
- [ ] `docker-compose.yml` with services:
  - `web` (Next.js on port 3000)
  - `api` (NestJS on port 4000)
  - `db` (PostgreSQL 16 on port 5432)
  - `redis` (Redis on port 6379)
  - `minio` (MinIO for local S3-compatible storage on port 9000)
- [ ] `docker-compose.dev.yml` overrides for development (hot reload, mounted volumes)
- [ ] `.dockerignore` files

### 1.1.7 — CI/CD Foundation
- [ ] GitHub Actions workflow: `ci.yml`
  - Lint (ESLint + Prettier check)
  - Type check (tsc --noEmit)
  - Unit tests (Vitest)
  - Build (both apps)
- [ ] Branch protection rules on `main`
- [ ] PR template

---

## Verification

```bash
# All of these should work when Phase 1.1 is complete:
pnpm install              # Install all dependencies
pnpm dev                  # Start all apps in development
pnpm build                # Build all apps
pnpm lint                 # Lint all packages
pnpm test                 # Run all tests
docker-compose up         # Start full stack in containers
```

---

## Notes

- Use Node.js 22 LTS (same as AIMS v1 — consistent with your environment)
- pnpm is preferred over npm/yarn for monorepo performance and strict dependency resolution
- All environment variables should have sensible defaults for local development
